// recuperer les coordonnees par le navigateur
navigator.geolocation.getCurrentPosition((data) => {
  console.log(`lat=${data.coords.latitude}`);
  document.cookie = `lat=${data.coords.latitude}`;
  console.log(`lat=${data.coords.longitude}`);
  document.cookie = `long=${data.coords.longitude}`;
  console.log( <%= cookies['lat'] %> );
  console.log( <%= cookies['long'] %> );
// calculer la ville correspondante en ruby avec la gem geocoder
<% results = Geocoder.search([cookies['lat'], cookies['long']]) %>
  const adress = <%= p results.first.city %>
  console.log(adress)
});
// remplacer cette ville dans le placeholder
